# leetcode
class Solution {
public:
    int removeElement(vector<int>& a, int k) {
       int n=a.size(); 
       int in=0;
       for(int i=0;i<n;i++){
           if(a[i] != k){
              a[in]=a[i];
              in++;
           }
       }
       return in;
    }
};
